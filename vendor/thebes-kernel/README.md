# Thebes kernel

Chimera builds against the Thebes kernel at commit `f7bfa41dbae1`.
The kernel's public repository is published separately; until then this directory names the dependency
and `tools/packages.sh` expects its source here.
